import {Theme} from 'styled-system';
import {colors, fonts, spaces, breakpoints} from '../tokens';

const customTheme: Theme = {
  fontSizes: fonts.fontSizes,
  space: spaces,
  colors,
  breakpoints,
};

export default customTheme;
